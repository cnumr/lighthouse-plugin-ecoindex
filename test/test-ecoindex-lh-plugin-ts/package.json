{
  "name": "@ecoindex-lh-test/plugin-core",
  "private": true,
  "version": "5.1.1",
  "type": "module",
  "description": "Tests avec le plugin en TS (avec `lighthouse-plugin-ecoindex-core`)",
  "scripts": {
    "pretest": "rimraf ./.lighthouseci",
    "test": "pnpm test:collect && pnpm verify && echo 'Test Done! 🚀'",
    "test:collect": "lhci collect",
    "verify": "node ../test-pages/verify-results.js ./.lighthouseci",
    "lhci": "lhci",
    "lhci:upload": "dotenv -- lhci upload --serverBaseUrl=http://localhost:9001 --token=$LHCI_TOKEN",
    "build": "echo \"No build because this is test project\"",
    "lint": "echo \"No lint because this is test project\"",
    "lint:fix": "echo \"No lint:fix because this is test project\"",
    "typecheck": "echo \"No typecheck because this is test project\"",
    "format:write": "echo \"No format:write because this is test project\"",
    "format:check": "echo \"No format:check because this is test project\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@lhci/cli": "0.15.1",
    "lighthouse": "13.3.0",
    "lighthouse-plugin-ecoindex-core": "workspace:*"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "rimraf": "^6.0.1"
  }
}
